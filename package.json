{
  "name": "stotra-sadhana",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first stotra and sahasranama memorization platform with mixed spaced repetition.",
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate-content.mjs",
    "build:site": "node scripts/build-site.mjs",
    "build": "npm run validate && npm test && npm run build:site"
  },
  "engines": {
    "node": ">=20"
  }
}
